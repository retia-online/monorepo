name: Publish Packages

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@core'

      - name: Install dependencies
        run: yarn install
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Build en orden de dependencias ────────────────────────────────────
      # configs y api primero (sin dependencias internas)
      # auth y ui después (dependen de api)
      - name: Build packages (en orden)
        run: |
          yarn workspace @core/configs build
          yarn workspace @core/api build
          yarn workspace @core/ui build
          yarn workspace @core/auth build
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Publicación 1: @core ──────────────────────────────────
      - name: Publish → @core
        run: |
          for pkg in configs api ui auth; do
            echo "📦 Publicando @core/$pkg ..."
            cd packages/$pkg
            npm publish || echo "⚠️  Ya publicado o falló ($pkg)"
            cd ../..
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Publicación 2: @retia-latam ───────────────────────────────────────
      - name: Publish → @retia-latam
        run: |
          for pkg in configs api ui auth; do
            echo "📦 Publicando @retia-latam/$pkg ..."
            cd packages/$pkg

            # Renombra el scope temporalmente
            node -e "
              const fs = require('fs');
              const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
              pkg.name = pkg.name.replace('@core', '@retia-latam');
              pkg.publishConfig = {
                registry: 'https://npm.pkg.github.com',
                '@retia-latam:registry': 'https://npm.pkg.github.com'
              };
              for (const dep of ['dependencies', 'devDependencies', 'peerDependencies']) {
                if (pkg[dep]) {
                  const updated = {};
                  for (const [k, v] of Object.entries(pkg[dep])) {
                    updated[k.replace('@core', '@retia-latam')] = v;
                  }
                  pkg[dep] = updated;
                }
              }
              fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
            "

            npm publish || echo "⚠️  Ya publicado o falló ($pkg)"
            cd ../..
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.RETIA_LATAM_TOKEN }}
