# npm configuration template
# This repository is self-contained and does not require private registry access

# Optional: Set registry for all packages (if you want to use a specific registry)
# registry=https://registry.npmjs.org/

# For development: you can use npm login for publishing
# npm login --registry=https://registry.npmjs.org/